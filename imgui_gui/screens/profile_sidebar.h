#ifndef PROFILE_SIDEBAR_H
#define PROFILE_SIDEBAR_H

#include "../core/app_state.h"

namespace ProfileSidebar {
    // Render user profile sidebar (desktop view)
    void renderSidebar(AppState& state);
}

#endif // PROFILE_SIDEBAR_H